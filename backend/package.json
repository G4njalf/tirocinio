{
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^16.5.0",
    "hardhat": "^2.23.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  },
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:token": "hardhat ignition deploy ./ignition/modules/MyToken.js --network sepolia",
    "deploy:Factory": "hardhat ignition deploy ./ignition/modules/InsuranceFactory.js --network sepolia"
  }
}
